async function checkValidBooking(dateId: number) {}

async function getBooking(userId: number) {}

async function createNewBooking(userId:number, dateId: number) {}

const bookingService = {
    getBooking,
    createNewBooking
};

export default bookingService