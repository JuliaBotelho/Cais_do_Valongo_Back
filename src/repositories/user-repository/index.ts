async function createUser(name:string,email:string, encryptedpassword:string) {
    
}

async function findUserById(userId:number) {
    
}

const userRepository = {
    createUser,
    findUserById
}

export default userRepository;